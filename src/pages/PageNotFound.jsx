import { Link } from 'react-router-dom';

export const PageNotFound = () => {
  return (
    <div>
      <Link to={'/'}>Home</Link>
      Page not Found!
    </div>
  );
};

// export default PageNotFound;
